log:
  level: INFO

api:
  dashboard: true
  insecure: true # dev only

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
